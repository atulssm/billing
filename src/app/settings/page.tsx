export default function SettingsPage() {
  return (
    <div className="space-y-4">
      <section className="space-y-2 rounded-xl border border-slate-800 bg-slate-900/60 p-3 text-xs text-slate-400">
        <p>
          For now this is just a placeholder screen so you can navigate the
          bottom menu. We&apos;ll add options like default payment method,
          preferred currency, and report filters as the MVP grows.
        </p>
      </section>
    </div>
  );
}

